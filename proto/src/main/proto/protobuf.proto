package medvedev.ilya.monitor.proto;

message Message {
    message SensorInfo {
        message SensorValue {
            required string name = 1;
            required float value = 2;
        }

        required string name = 1;
        repeated SensorValue value = 2;
    }

    required int64 time = 1;
    repeated SensorInfo value = 2;
}